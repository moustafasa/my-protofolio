include ../helpers/variables
include ../helpers/mixins
doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title my protofolio
        block styles 
            link(rel="stylesheet", href=`${stylesPath}/all.min.css`)
            link(rel="stylesheet", href=`${stylesPath}/normalize.css`)
    body
        include ./header
        block pageContent 
        block scripts 